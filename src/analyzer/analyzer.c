// analyzer.c
// For now, this just prints that a file is being analyzed.

#include <stdio.h>
#include "analyzer.h"

void analyze_file(const char *filepath, AnalysisResult *result) {
    // Placeholder behavior: just print the file name
    printf("Analyzed: %s\n", filepath);
}
